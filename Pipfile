[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
langgraph = ">=0.2.0"
aiofiles = ">=24.0.0"
playwright = ">=1.40.0"
python-dotenv = ">=1.0.0"
instructor = ">=1.2.0"
anthropic = ">=0.26.0"  # Add this line

[dev-packages]

[requires]
python_version = "3.11"

